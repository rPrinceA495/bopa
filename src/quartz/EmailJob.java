package quartz;

import java.util.ArrayList;

import model.DAO.ItemDAO;

/**THIS JOB IS CALLED UPON DAILY AT 00:00
 * 
 * @author rpa495
 *
 */
public class EmailJob //implements //Job 

{

	/*
	@Override
	public void execute(Object o) throws Exception {
		
		ItemDAO id;
		
		long time = System.currentTimeMillis();
		
		// Todays date
		java.sql.Date date = new java.sql.Date(time);
		
		// Calendar instance to add 5 days to today and retrieve date:
		
		//Mutate to 5 days from today
		
		id = new ItemDAO();
		
		// Use ItemDAO method to get email addresses of those seller's whose item is about to expire.
		ArrayList<String> emails = id.getReminderEmails(date);
		
		// Proceed to send email that property is about to expire
		
	}
	
	*/
	
}
